#include "main.h"

/**
 * _printf - produces output acording to a format
 * @format: format specifier
 * Return: 0 after successful execution and 1 otherwise
 */

int _printf(const char *format, ...)
{
	va_list arg_ptr;

	va_start(arg_ptr, format);

	return (0);
}
